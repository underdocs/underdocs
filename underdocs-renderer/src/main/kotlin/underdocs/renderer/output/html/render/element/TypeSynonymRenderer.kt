package underdocs.renderer.output.html.render.element

import j2html.tags.Tag
import underdocs.renderer.representation.TypeSynonym

class TypeSynonymRenderer {
    fun render(typeSynonym: TypeSynonym): Tag<*> {
        TODO()
    }
}